import { BookType } from "@/pages/BookOverview/types";

type ChapterReferenceOverviewPropsType = {
  language: string;
  languageCode: string;
  bookTitle: string;
  bookInfo: BookType;
}

export type {
  ChapterReferenceOverviewPropsType,
}